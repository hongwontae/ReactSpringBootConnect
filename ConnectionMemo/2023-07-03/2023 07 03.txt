서비스 개발까지 했다. => 서비스를 테스트해야된다

rest api는 표준이 없다.

리액트의 결과는 HTML이다.

React + SpringBoot

vs코드를 두개로 하면 에바니까.. 


askink 함수의 결과는 항상 promise이다. => 약속 어음이다.

리액트로 데이터를 받기
  
부트는 8080port로, react는 3000port로 port가 다르다 => 처리는 됐는데 포트가 달라서 데이터 처리를 막는다. 

그러나 json 서버는 다 들어오게 해준다.

포트가 다르면 허락 메시지가 필요하다. AXIOS는 CORS통신이다. 

cors가 중요하다.

PRG 패턴을 벗어나는 경우이다.

input에게 changeView를 전달한다.




	




















